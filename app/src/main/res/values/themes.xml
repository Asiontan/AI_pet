<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Pet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Pet" parent="Base.Theme.Pet" />

    <!-- 聊天弹窗：窗口背景透明，保留状态栏 -->
    <style name="Theme.Pet.ChatDialog" parent="Theme.Pet">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowAnimationStyle">@style/ChatDialogAnim</item>
    </style>

    <style name="ChatDialogAnim">
        <item name="android:windowEnterAnimation">@anim/chat_slide_up</item>
        <item name="android:windowExitAnimation">@anim/chat_slide_down</item>
    </style>
</resources>